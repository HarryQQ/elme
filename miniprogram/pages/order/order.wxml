<!--index.wxml-->
<view class="container">
  <view bindtap="flash">刷新</view>
  <view wx:for="{{list}}" wx:key="{{index}}">
    <view class="item">
      <view>{{item.nickName}}点了{{item.name}}</view>
      <view>{{item.price}}元</view>
    </view>
  </view>
</view>
